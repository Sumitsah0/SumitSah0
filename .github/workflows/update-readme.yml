name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: actions/checkout@v2
      - uses: Sumitsah0/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ 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 }}
